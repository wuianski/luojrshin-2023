import { Merriweather } from 'next/font/google'

export const merriweather700  = Merriweather({ subsets: ['latin'], weight: '700' })
export const merriweather300  = Merriweather({ subsets: ['latin'], weight: '300' })